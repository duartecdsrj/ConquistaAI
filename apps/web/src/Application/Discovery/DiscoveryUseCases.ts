import type{DiscoveryRepository,DiscoveryResource,DiscoveryResourceType}from '../../Domain/Discovery/DiscoveryRepository';export class ListDiscoveryResourcesUseCase{public constructor(private readonly repository:DiscoveryRepository){}public execute(type?:DiscoveryResourceType):Promise<readonly DiscoveryResource[]>{return this.repository.list(type)}}export class SearchDiscoveryUseCase{public constructor(private readonly repository:DiscoveryRepository){}public execute(query:string,type:DiscoveryResourceType):Promise<readonly DiscoveryResource[]>{if(query.trim().length<3)return Promise.reject(new Error('A consulta deve ter ao menos 3 caracteres.'));return this.repository.search(query.trim(),type)}}
