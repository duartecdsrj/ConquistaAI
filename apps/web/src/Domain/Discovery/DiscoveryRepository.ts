export type DiscoveryResourceType='EXAM'|'ANSWER_KEY';export interface DiscoveryResource{readonly id:string;readonly type:DiscoveryResourceType;readonly title:string;readonly sourceUrl:string;readonly provider:string;readonly queryText:string;readonly discoveredAt:string}export interface DiscoveryRepository{list(type?:DiscoveryResourceType):Promise<readonly DiscoveryResource[]>;search(query:string,type:DiscoveryResourceType):Promise<readonly DiscoveryResource[]>}
