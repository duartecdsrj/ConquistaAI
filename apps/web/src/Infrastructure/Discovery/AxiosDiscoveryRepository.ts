import type{DiscoveryRepository,DiscoveryResource,DiscoveryResourceType}from '../../Domain/Discovery/DiscoveryRepository';import{getData,postData}from '../Http/AxiosApiClient';export class AxiosDiscoveryRepository implements DiscoveryRepository{public list(type?:DiscoveryResourceType):Promise<readonly DiscoveryResource[]>{return getData('/admin/discovery/resources',{params:type?{type}:undefined})}public search(query:string,type:DiscoveryResourceType):Promise<readonly DiscoveryResource[]>{return postData('/admin/discovery/searches',{query,resource_type:type})}}
